// Generated by CoffeeScript 1.10.0
(function() {
  $(window).load(function() {
    return $('.box').each(function() {
      var $box, $img, boxHeight, boxWidth, imgHeight, imgWidth;
      $box = $(this);
      $img = $box.find('img');
      boxWidth = $box.width();
      boxHeight = $box.height();
      imgWidth = $img.width();
      imgHeight = $img.height();
      $img.drag((function(e, arg) {
        var offsetX, offsetY;
        offsetX = arg.offsetX, offsetY = arg.offsetY;
        offsetX = Math.min(0, offsetX);
        offsetY = Math.min(0, offsetY);
        offsetX = Math.max(offsetX, boxWidth - $img.width());
        offsetY = Math.max(offsetY, boxHeight - $img.height());
        return $(this).css({
          top: offsetY,
          left: offsetX
        });
      }), {
        relative: true
      });
      return $img.on('mousewheel', function(e) {
        var step;
        step = 1.1;
        if (e.originalEvent.wheelDelta < 0) {
          step = 1 / step;
        }
        return $img.width($img.width() * step);
      });
    });
  });

}).call(this);

//# sourceMappingURL=pan.js.map
